---
description: Keep operator-facing docs in sync when UX or product behaviour changes
alwaysApply: true
---

# User documentation

## Canonical file

- **`docs/USER_GUIDE.md`** — how operators use the app (not Docker or API internals).

## When to update

If the change affects **what someone does in the browser** (new route, setting, clock behaviour, template workflow, renamed nav label, or a removed feature), update **`docs/USER_GUIDE.md`** in the **same change** when practical.

## Also touch

- **`docs/README.md`** — add or adjust the index row if you add a **new** top-level doc under `docs/`.
- Root **`README.md`** — add a pointer under **Documentation** if operators need to find the new doc.

## Do not duplicate

- Install / Compose / ports → link **`README.md`** / **`docs/DEVELOPMENT.md`** from the user guide instead of copying.
- Deep engineering → **`docs/REALTIME.md`**, **`docs/DECISIONS.md`**, **`AGENTS.md`**.

## Patch template presets

Patch workbooks come from **uploaded** library templates or **Create blank template** (Settings). `<Workbook>` mounts with a trivial placeholder; the Yjs opLog replay sets the real structure immediately. There is no client-side "seed" decoding — the WebSocket sync is the single path. If you change the template creation / Yjs snapshot flow, update **`web/src/lib/patchWorkbookCollab.ts`** and **`docs/USER_GUIDE.md`** (templates).

## Process detail

See **`docs/DEVELOPMENT.md`** § *Documentation maintenance* for the full split of audiences and review habits.
