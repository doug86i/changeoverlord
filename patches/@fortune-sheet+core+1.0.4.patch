diff --git a/node_modules/@fortune-sheet/core/dist/index.esm.js b/node_modules/@fortune-sheet/core/dist/index.esm.js
index 5c9820b..e1ab1d0 100644
--- a/node_modules/@fortune-sheet/core/dist/index.esm.js
+++ b/node_modules/@fortune-sheet/core/dist/index.esm.js
@@ -67143,7 +67143,7 @@ function addSheet(ctx, settings) {
   var isPivotTable = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : false;
   var sheetName = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : undefined;
   var sheetData = arguments.length > 5 && arguments[5] !== undefined ? arguments[5] : undefined;
-  if (ctx.allowEdit === false) {
+  if (ctx.allowEdit === false && _.isNil(sheetData)) {
     return;
   }
   var order = ctx.luckysheetfile.length;
@@ -67186,9 +67186,6 @@ function addSheet(ctx, settings) {
 }
 function deleteSheet(ctx, id) {
   var _ctx$hooks$beforeDele, _ctx$hooks5;
-  if (ctx.allowEdit === false) {
-    return;
-  }
   var arrIndex = getSheetIndex(ctx, id);
   if (arrIndex == null) {
     return;
@@ -70187,6 +70184,8 @@ function handleOverlayTouchStart(ctx, e, globalCache) {
   globalCache.touchMoveStartPos = {
     x: touch.pageX,
     y: touch.pageY,
+    initialScrollLeft: ctx.scrollLeft,
+    initialScrollTop: ctx.scrollTop,
     vy: 0,
     moveType: "y"
   };
@@ -70198,10 +70197,8 @@ function handleOverlayTouchMove(ctx, e, globalCache, scrollbarX, scrollbarY) {
     if (!globalCache.touchMoveStartPos) return;
     var slideX = touch.pageX - globalCache.touchMoveStartPos.x;
     var slideY = touch.pageY - globalCache.touchMoveStartPos.y;
-    var scrollLeft = ctx.scrollLeft;
-    var scrollTop = ctx.scrollTop;
-    scrollLeft -= slideX;
-    scrollTop -= slideY;
+    var scrollLeft = globalCache.touchMoveStartPos.initialScrollLeft - slideX;
+    var scrollTop = globalCache.touchMoveStartPos.initialScrollTop - slideY;
     scrollbarY.scrollTop = scrollTop;
     globalCache.touchMoveStartPos.vy_y = slideY;
     globalCache.touchMoveStartPos.scrollTop = scrollTop;
@@ -75960,9 +75957,14 @@ function escapeHTMLTag(str) {
   return str.replace(/</g, "&lt;").replace(/>/g, "&gt;");
 }
 function getSheetIndex(ctx, id) {
+  if (id == null) {
+    return null;
+  }
+  var idStr = String(id);
   for (var i = 0; i < ctx.luckysheetfile.length; i += 1) {
     var _ctx$luckysheetfile$i;
-    if (((_ctx$luckysheetfile$i = ctx.luckysheetfile[i]) === null || _ctx$luckysheetfile$i === void 0 ? void 0 : _ctx$luckysheetfile$i.id) === id) {
+    var sid = (_ctx$luckysheetfile$i = ctx.luckysheetfile[i]) === null || _ctx$luckysheetfile$i === void 0 ? void 0 : _ctx$luckysheetfile$i.id;
+    if (sid != null && String(sid) === idStr) {
       return i;
     }
   }
diff --git a/node_modules/@fortune-sheet/core/dist/index.js b/node_modules/@fortune-sheet/core/dist/index.js
index 04113a1..b585ad5 100644
--- a/node_modules/@fortune-sheet/core/dist/index.js
+++ b/node_modules/@fortune-sheet/core/dist/index.js
@@ -67153,7 +67153,7 @@ function addSheet(ctx, settings) {
   var isPivotTable = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : false;
   var sheetName = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : undefined;
   var sheetData = arguments.length > 5 && arguments[5] !== undefined ? arguments[5] : undefined;
-  if (ctx.allowEdit === false) {
+  if (ctx.allowEdit === false && ___default['default'].isNil(sheetData)) {
     return;
   }
   var order = ctx.luckysheetfile.length;
@@ -67196,9 +67196,6 @@ function addSheet(ctx, settings) {
 }
 function deleteSheet(ctx, id) {
   var _ctx$hooks$beforeDele, _ctx$hooks5;
-  if (ctx.allowEdit === false) {
-    return;
-  }
   var arrIndex = getSheetIndex(ctx, id);
   if (arrIndex == null) {
     return;
@@ -70197,6 +70194,8 @@ function handleOverlayTouchStart(ctx, e, globalCache) {
   globalCache.touchMoveStartPos = {
     x: touch.pageX,
     y: touch.pageY,
+    initialScrollLeft: ctx.scrollLeft,
+    initialScrollTop: ctx.scrollTop,
     vy: 0,
     moveType: "y"
   };
@@ -70208,10 +70207,8 @@ function handleOverlayTouchMove(ctx, e, globalCache, scrollbarX, scrollbarY) {
     if (!globalCache.touchMoveStartPos) return;
     var slideX = touch.pageX - globalCache.touchMoveStartPos.x;
     var slideY = touch.pageY - globalCache.touchMoveStartPos.y;
-    var scrollLeft = ctx.scrollLeft;
-    var scrollTop = ctx.scrollTop;
-    scrollLeft -= slideX;
-    scrollTop -= slideY;
+    var scrollLeft = globalCache.touchMoveStartPos.initialScrollLeft - slideX;
+    var scrollTop = globalCache.touchMoveStartPos.initialScrollTop - slideY;
     scrollbarY.scrollTop = scrollTop;
     globalCache.touchMoveStartPos.vy_y = slideY;
     globalCache.touchMoveStartPos.scrollTop = scrollTop;
@@ -75970,9 +75967,14 @@ function escapeHTMLTag(str) {
   return str.replace(/</g, "&lt;").replace(/>/g, "&gt;");
 }
 function getSheetIndex(ctx, id) {
+  if (id == null) {
+    return null;
+  }
+  var idStr = String(id);
   for (var i = 0; i < ctx.luckysheetfile.length; i += 1) {
     var _ctx$luckysheetfile$i;
-    if (((_ctx$luckysheetfile$i = ctx.luckysheetfile[i]) === null || _ctx$luckysheetfile$i === void 0 ? void 0 : _ctx$luckysheetfile$i.id) === id) {
+    var sid = (_ctx$luckysheetfile$i = ctx.luckysheetfile[i]) === null || _ctx$luckysheetfile$i === void 0 ? void 0 : _ctx$luckysheetfile$i.id;
+    if (sid != null && String(sid) === idStr) {
       return i;
     }
   }
