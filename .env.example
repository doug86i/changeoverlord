# Copy to .env and adjust. Compose loads .env automatically.

# Root directory for all persistent data (PostgreSQL, Redis, uploads).
# Default when unset: ./data (relative to the compose file).
# Example: point at another disk or NAS mount:
# DATA_DIR=/mnt/storage/changeoverlord
DATA_DIR=./data
