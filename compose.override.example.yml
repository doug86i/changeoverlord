# Optional: Docker merges `docker-compose.override.yml` with `docker-compose.yml`
# automatically (rename this file to `docker-compose.override.yml`; it is gitignored).
#
# Prefer **`.env`** (`HOST_PORT`, `DATA_DIR`, …) — see **`.env.example`** — for most cases.
#
# Building the app from source uses **`docker-compose.dev.yml`** via **`make dev`**, not this file.

services:
  app:
    ports:
      - "8080:80"
