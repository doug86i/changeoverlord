.DS_Store
.env
.env.*
!.env.example
# Runtime data (structure documented in data/README.md)
data/db/
data/uploads/
data/logs/
# Stray Redis volume if ever created locally — not part of the stack
data/redis/
# Legacy client-debug path (before DATA_DIR/logs); safe to delete if empty
logs/
node_modules/
# tsc incremental (local + Docker BuildKit cache mount)
api/.cache/
dist/
build/
*.log
coverage/
.idea/
.vscode/
docker-compose.override.yml
# Local Docker build-input stamps (scripts/docker-build-gate.sh)
.docker/
# Scratch workbook extractions (not versioned)
examples/*_live.json
examples/*_corrected.json
# Excel lock files
~$*
