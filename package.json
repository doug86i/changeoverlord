{
  "name": "changeoverlord",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "build": "npm run build -w web && npm run build -w api",
    "build:test": "npm run build -w api && npm run build -w web",
    "docker:build:app": "docker compose -f docker-compose.yml -f docker-compose.dev.yml build app",
    "dev": "npm run dev -w api & npm run dev -w web",
    "postinstall": "patch-package"
  },
  "devDependencies": {
    "patch-package": "^8.0.0"
  },
  "overrides": {
    "yjs": "13.6.30",
    "@y/protocols": "1.0.6-1",
    "@y/websocket-server": {
      "yjs": "13.6.30",
      "@y/protocols": "1.0.6-1"
    }
  }
}
